#ifndef DBRECORD_H
#define DBRECORD_H
/*
#include <QVariantList>

class DBRecord : public QVariantList
{
public:
    DBRecord(): QVariantList() {}
    bool empty() { return count() < 1; }
    QVariant const &field(const int index) const { return index < count() ? at(index) : *new QVariant(); }
};
*/
#endif // DBRECORD_H
